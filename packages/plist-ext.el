;; -*- lexical-binding: t; -*-

(defmacro plist-ext-get-flag (plist key)
  ;; TODO: documentation string
  (let ()
    `(let ((p (plist-member props )))
       )))

(provide 'plist-ext)
;;; plist-ext.el ends here
